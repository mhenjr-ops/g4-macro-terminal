name: Daily G4 Macro Brief

on:
  schedule:
    # 10:00 UTC weekdays — ahead of the 12:30/13:30 UTC US data window, so the
    # pre-event calls are published BEFORE the prints they forecast.
    - cron: "0 10 * * 1-5"
  workflow_dispatch:        # lets you run it by hand from the Actions tab

permissions:
  contents: write           # needed to commit the regenerated brief

concurrency:
  group: daily-brief
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install SDK
        run: pip install --quiet anthropic

      - name: Build brief
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python scripts/build_forecast.py

      - name: Commit if changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/forecast.json data/ledger.json
          if git diff --staged --quiet; then
            echo "No change — nothing to commit."
          else
            git commit -m "Forecast $(date -u +%Y-%m-%d)"
            git push
          fi
